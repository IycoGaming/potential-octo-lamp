{
  "name": "dsv2",
  "version": "2.0.0",
  "private": true,
  "description": "DSV2 productivity app wrapped with Capacitor.",
  "scripts": {
    "serve": "npx serve www",
    "sync:android": "cap sync android",
    "open:android": "cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0"
  }
}